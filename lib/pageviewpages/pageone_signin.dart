import 'package:flutter/material.dart';

class PageoneSignin extends StatelessWidget {
  const PageoneSignin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}